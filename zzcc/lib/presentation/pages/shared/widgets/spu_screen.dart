// Shared processing unit
import 'package:flutter/material.dart';

class SPUScreen extends StatelessWidget {
  const SPUScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('共享计算单元功能'),
    );
  }
}