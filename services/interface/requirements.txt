# FastAPI / ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.12.0
pydantic-settings>=2.6.0

# NebulaGraph
nebula3-python>=3.6.0

# Structured logging
structlog>=25.0.0

# HTTP / file parsing
httpx>=0.28.0
python-multipart>=0.0.20
pypdf>=4.0.0
python-docx>=1.1.0

# Postgres (KnowledgeTable) + Redis
asyncpg>=0.30.0
sqlalchemy[asyncio]>=2.0.0
redis>=5.0.0

# Rate limiting
slowapi>=0.1.9

# Retry / resilience
tenacity>=9.0.0

# Prometheus metrics (optional, enable via METRICS_ENABLED=1)
prometheus-client>=0.21.0
